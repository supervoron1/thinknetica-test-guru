<tr>
  <td><%= link_to get_short_question(gist.question.body), admin_question_path(gist.question) %></td>
  <td><%= link_to gist_hash(gist.url), gist.url %></td>
  <td><%= gist.user.email %></td>
</tr>